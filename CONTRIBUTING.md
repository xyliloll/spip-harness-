# Contributing to SPIP

感谢关注 SPIP。完整贡献流程将在 `v1.0.0` 发布时启用。

## 基本原则

- 先阅读 README、架构说明和对应 Issue。
- 优先修复真实纵向用户闭环，不重复开发同名链路。
- 区分代码存在、自动测试通过、浏览器验证和真实端到端完成。
- 不用 Mock 成功冒充真实 API、模型或 Tool 成功。
- 不提交 `.env`、密钥、密码、用户文件、数据库或私有日志。
- 涉及高风险写入、敏感数据和权限边界时必须失败关闭。

## 提交前检查

- 运行受影响模块的定向测试；
- 运行 Python 编译或前端生产构建；
- 检查 Migration 和数据库行为；
- 执行 `git diff --check`；
- 对用户可见功能完成真实浏览器和刷新恢复验收；
- 清理临时输出和调试日志。

大规模架构改动请先建立 Issue 说明产品问题、替代方案、迁移路径和回退方式。
